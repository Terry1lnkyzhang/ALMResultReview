{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["decisions"],
  "properties": {
    "decisions": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["review_step", "role", "required", "selected_equipment_ids", "reason"],
        "properties": {
          "review_step": {"type": "integer", "minimum": 1},
          "role": {"enum": ["controlled_equipment", "dut_or_other", "uncertain"]},
          "required": {"type": "boolean"},
          "selected_equipment_ids": {"type": "array", "items": {"type": "string"}},
          "reason": {"type": "string", "minLength": 1, "maxLength": 300}
        }
      }
    }
  }
}
